## Behavioral Rules
* **Question-Only Mode**: If the user's prompt starts with "질문:" (or similar expressions like "질문::", "질문 :"), you MUST NOT execute any tools that modify source code, write files, or execute state-changing terminal commands. You must ONLY provide a text response answering the user's question.
